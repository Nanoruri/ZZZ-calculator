{
  "compilerOptions": {
    "jsx": "react",                        // JSX를 React로 변환
    "esModuleInterop": true,               // ES 모듈과 CommonJS 간 호환을 위해 설정
    "moduleResolution": "node",            // Node.js 방식으로 모듈 해석
    "skipLibCheck": true,                  // 라이브러리 타입 체크 생략 (속도 향상)
    "forceConsistentCasingInFileNames": true,
    "allowImportingTsExtensions": true,    // TS 확장자를 생략해도 자동으로 찾아줌
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "test/**/*.ts",
    "test/**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}
